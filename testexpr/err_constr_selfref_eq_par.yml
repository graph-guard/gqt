schema: >
  type Query { f(a: Int): Int }

template: >
  query { f(a=$a: ($a)) }

expect-errors:
  - '1:18: argument self reference in constraint'

expect-errors(schemaless):
  - '1:18: argument self reference in constraint'
